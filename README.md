# fashion-mnist-image-classification
A convolutional neural network trained on the Fashion-MNIST dataset for clothing classification. Includes preprocessing, 5-fold cross-validation, model saving, and real-image prediction with automatic resizing and grayscale conversion.
